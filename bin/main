#!/bin/bash

# Set current dir to the app dir and load .env files
source $(dirname ${BASH_SOURCE[0]})/init || exit 1

# Clean up old uploads
./bin/cleanup || exit 1

# Create zip file
./bin/create-zip || exit 1

# Upload zip file
./bin/upload-zip || exit 1

exit 0
